# Игнорируем пакетные зависимости npm
node_modules/

# Игнорируем конфигурационные блокировки пакетов
package-lock.json

# Можно добавить другие специфические для проекта исключения, например:
logs/*.log
temp/*.tmp
thumbs.db